## 详解
Long Polling(长轮询),和Polling(轮询),这都是拉取数据的两种模式.
Polling是不管服务端数据有误更新,客户端每个隔定长时间拉取一次数据,可能有新的数据返回,也可能什么都没有

Long Polling相比Polling,客户端发起Long Polling,此时如果服务端没有相关数据,会hold住请求,知道服务端有相关数据返回,或者是等一定时间超时才会有返回.返回后客户端优惠再次发起下一次Long Polling,这种方式是对拉模式的一个优化,解决了拉取消息不及时以及大量消耗轮询次数.

